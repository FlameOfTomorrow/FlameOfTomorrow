PUSH
ORG $9cd10
POIN NewPrepScreenItemsProc
ORG $9cb74
BL(CheckExpandedPrepItemIDsWrapper)
POP


#include "ExpandedPrepItemEffect.event"

MESSAGE PrepItemEffectWrapper

ALIGN 4
PrepItemEffectTable:
ORG currentOffset+(0xFF*4)

#define SetPrepItemEffect(itemID,function) "PUSH; ORG PrepItemEffectTable+(itemID*4); POIN function; POP"

SetPrepItemEffect(AdeptManual,AdeptScrollEffect)
